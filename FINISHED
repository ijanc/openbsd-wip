# One line per port ready to go into OpenBSD. Remove the line and the
# port once it is committed upstream.
#
# Format: pkgpath			message (owner)

x11/rxvt-unicode-ligatures	ready for import, tested on amd64 -current, not sent to ports@ (ijanc)
x11/urxvt-perls			ready for import, tested on amd64 -current, not sent to ports@ (ijanc)
devel/cargo-generate		ready for import, tested on amd64 -current, not sent to ports@ (ijanc)
